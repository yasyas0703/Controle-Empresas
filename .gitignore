# dependencies
node_modules/
.pnp
.pnp.js

# build
.next/
out/
build/
dist/

# env / secrets — NENHUM arquivo .env vai pro git
.env*
# doc de remediação que continha chave real — fora do versionamento
FASE-0-ROTACAO-SECRETS.md

# debug
npm-debug.log*
yarn-debug.log*
dev-output.log

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# temp
tmpclaude-*
nul


comand.md

# guias fiscais reais — dados sensíveis (CNPJs, valores, razões sociais)
FISCAL/

# planilhas e outputs com CNPJs reais e códigos de receita por empresa
CODIGOS EMPRESAS.xlsx
scripts/output-*.csv

# scratch local por máquina (convenção: prefixo _ em scripts/ = dados reais e
# análises por máquina — não versionar)
scripts/_*

# watcher local (estado e logs por máquina — não devem ser versionados)
scripts/.watcher-state.json
scripts/.watcher.log

# TypeScript build cache (regenerado a cada build)
tsconfig.tsbuildinfo

cadastro/
scripts/.watcher-certidoes-state.json
scripts/.watcher-certidoes.log
scripts/.watcher-certidoes-run.log
scripts/data/
